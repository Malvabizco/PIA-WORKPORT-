<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PIA_ClaseOrdinaria"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".SetPasswordActivity" />
        <activity android:name=".VerifyEmailActivity" />
        <activity android:name=".PendingActivationActivity" />
        <activity android:name=".AdminDashboardActivity" />
        <activity android:name=".GuardDashboardActivity" />
        <activity android:name=".PendingUsersActivity" />
        <activity android:name=".AdminComplaintsActivity" />
        <activity android:name=".AdminPostAnnouncementActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".ReservationsActivity" />
        <activity android:name=".AnnouncementsActivity" />
        <activity android:name=".ComplaintsActivity" />
        <activity android:name=".AccessControlActivity" />

        <!-- Actividad para el escáner de QR -->
        <activity
            android:name="com.journeyapps.barcodescanner.CaptureActivity"
            android:screenOrientation="fullSensor"
            tools:replace="android:screenOrientation" />

    </application>

</manifest>
